#include <stddef.h>
/**
 * _strspn - Computes the length of the initial segment of a string
 * consisting entirely of characters from another string
 * @s: The string to be scanned
 * @accept: The string containing the characters to match
 *
 * Return: The number of characters in the initial segment of @s which consist
 * only of characters from @accept.
 */
unsigned int _strspn(char *s, char *accept)
{
	/* Implementation goes here */
}
