#include <stddef.h>
/**
 * _isalpha - Checks if a character is an alphabetic character.
 * @c: The character to be checked.
 * Return: Returns 1 if @c is an alphabetic character, 0 otherwise.
 */
int _isalpha(int c)
{
	/* Implement the function here */
	return (0);
}
