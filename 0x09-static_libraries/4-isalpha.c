#include "main.h"
/**
 * _isalpha - checks whether a given integer represents an alphabetic character
 * @c: the integer value to check
 *
 * Return: 1 if @c is an alphabetic , 0 otherwise
 */
int _isalpha(int c)
{
	return (0);
}
